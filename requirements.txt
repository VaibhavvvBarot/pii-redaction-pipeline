# PII De-Identification Pipeline Requirements

# Core ASR
faster-whisper>=1.0.0

# Audio processing
soundfile>=0.12.0
numpy>=1.24.0

# Data handling
huggingface-hub>=0.20.0

# PyTorch (for Whisper, install appropriate version for your platform)
torch>=2.0.0

# Optional: for progress bars
tqdm>=4.65.0
